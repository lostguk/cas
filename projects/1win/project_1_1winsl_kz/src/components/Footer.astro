---
const affiliateLink = '#';
const year = new Date().getFullYear();

const sections = [
  {
    title: 'Разделы',
    links: [
      { href: '/registraciya', label: 'Регистрация' },
      { href: '/skachat', label: 'Скачать' },
      { href: '/casino', label: 'Казино' },
    ],
  },
  {
    title: 'Полезное',
    links: [
      { href: '/bonus', label: 'Бонусы' },
      { href: '/sweet-bonanza', label: 'Sweet Bonanza' },
      { href: '/coin-strike', label: 'Coin Strike' },
    ],
  },
  {
    title: 'Информация',
    links: [
      { href: '/about', label: 'Об авторе' },
    ],
  },
];
---

<footer class="mt-16 border-t border-cream-200 bg-cream-100">
  <div class="max-w-2xl mx-auto px-4 sm:px-6 py-12">
    <div class="grid grid-cols-2 sm:grid-cols-4 gap-8">
      <div class="col-span-2 sm:col-span-1">
        <span class="font-heading text-lg font-bold text-cream-900">
          1<span class="text-violet-500">Win</span>
        </span>
        <p class="text-sm text-cream-500 mt-3 leading-relaxed">
          Личный блог Серика из Тараза. Партнёрские ссылки, не официальный сайт.
        </p>
      </div>
      {sections.map((sec) => (
        <div>
          <h3 class="text-sm font-semibold text-cream-700 mb-3">{sec.title}</h3>
          <ul class="space-y-2">
            {sec.links.map((l) => (
              <li>
                <a href={l.href} class="text-sm text-cream-500 hover:text-violet-600 transition-colors">
                  {l.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
    <div class="mt-10 pt-6 border-t border-cream-200 text-xs text-cream-400 space-y-1">
      <p>21+. Играй ответственно. Азартные игры могут вызывать зависимость.</p>
      <p>Информационный ресурс с партнёрскими ссылками, не является официальным сайтом.</p>
      <p class="text-cream-500">© {year} 1winsl.kz · Серик Калиев</p>
    </div>
  </div>
</footer>
