---
interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className = '', hover = true } = Astro.props;
---

<div
  class:list={[
    'ring-1 ring-white/10 p-6',
    hover && 'hover:ring-brand-500/50 transition-colors duration-100',
    className,
  ]}
>
  <slot />
</div>
