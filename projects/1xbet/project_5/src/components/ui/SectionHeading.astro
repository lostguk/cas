---
interface Props {
  tag?: 'h2' | 'h3';
  align?: 'left' | 'center';
  class?: string;
}

const { tag: Tag = 'h2', align = 'center', class: className = '' } = Astro.props;
const alignClass = align === 'center' ? 'text-center' : 'text-left';
---

<div class:list={[alignClass, className]}>
  <Tag class="text-2xl sm:text-3xl lg:text-4xl font-semibold text-gray-900 leading-tight">
    <slot />
  </Tag>
  <slot name="subtitle" />
</div>
